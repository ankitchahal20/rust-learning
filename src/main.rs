#![allow(dead_code)] // allows used variable or function
#![allow(unused_imports)]

// main function is the entrypoint for RUST application

fn main(){
    println!("Hello from Rust")
    // println is a macro which outputs the argument it to the terminal in this case.
}
